$('.comments-box .comment-form').replaceWith('<%= escape_javascript(render('account/comments/form', :commentable => parent, :comment => Comment.new)) %>');
$('<%= escape_javascript(render('account/comments/comment', :comment => @comment)) %>').hide().prependTo('.comments-box .comments');
$('.comments-box .comments .empty').remove();
$('.comments-box .comments .comment:first').fadeIn(5000);