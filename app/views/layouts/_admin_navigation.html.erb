<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <%= link_to admin_area_title, admin_root_path, :class => 'brand' %>
      <div class="container-fluid nav-collapse">
        <% if admin_signed_in? %>
          <ul class="nav">
            <%= navigation_item(:accounts, admin_accounts_path) %>
          </ul>

          <div class="nav pull-right">
            <a href="#" class="btn dropdown-toggle" data-toggle="dropdown">
              <i class="icon-user"></i> <%= current_admin %> <b class="caret"></b>
            </a>

            <ul class="dropdown-menu">
              <li><%= link_to t('helpers.actions.edit_profile'), edit_admin_profile_path %></li>
              <li><%= link_to t('helpers.actions.sign_out'), destroy_admin_session_path, :method => 'delete' %></li>
            </ul>
          </div>
        <% end %>

        <ul class="nav pull-right">
          <li><%= link_to t('helpers.links.go_to_website'), root_path, :target => '_blank' %></li>
        </ul>
      </div>
    </div>
  </div>
</div>