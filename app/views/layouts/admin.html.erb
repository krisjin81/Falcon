<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : admin_area_title %></title>
  <%= stylesheet_link_tag    "admin", :media => "all" %>
  <%= javascript_include_tag "admin" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="wrapper">
    <%= render 'layouts/admin_navigation' %>
    <div class="container-fluid">
      <div class="content">
        <%= render :partial => 'admin/breadcrumbs' %>
        <%= render :partial => 'layouts/messages' %>
        <%= yield %>
      </div>
    </div>
    <div class="push"></div>
  </div>

  <footer>
    <div class="container">
      <p class="brand"><%=raw t('application.copyright') %></p>
    </div>
  </footer>
</body>
</html>
