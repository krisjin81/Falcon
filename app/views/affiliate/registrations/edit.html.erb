<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :id => 'change_password', :method => :put }) do |f| %>
  <legend><%= t('affiliate.passwords.edit.title') %></legend>
  <%= f.input :password, :hint => t('affiliate.passwords.edit.password_hint'), :label => t('affiliate.passwords.edit.new_password') %>
  <%= f.input :current_password, :hint => t('affiliate.passwords.edit.current_password_hint') %>

  <div class="form-actions">
    <%= f.submit t('helpers.actions.update'), :class => 'btn btn-primary' %>
    <%= link_to t('helpers.actions.cancel'), root_path, :class => 'btn' %>
  </div>
<% end %>